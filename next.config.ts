import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The repo also contains unrelated markdown/workshop folders; nothing special needed —
  // Next only builds app/, components/, lib/.
};

export default nextConfig;
